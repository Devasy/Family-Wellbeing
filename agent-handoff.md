System Context & PersonaAct as an expert Mobile Software Engineer specializing in Flutter, Kotlin, and Android OS integration.The user wants to build a "Family Digital Wellbeing" Android application.They have a Flutter environment set up but want to avoid opening Android Studio. Your goal is to initialize the project, write the code, and provide terminal commands to build the APK.Project ArchitectureFramework: Flutter (for the UI and routing).Native Android Layer (Kotlin): Handles Android's UsageStatsManager and WorkManager for background synchronization.Database: MongoDB Atlas.Design System: Minimalist, Notion/Claude-inspired (Zinc greys, stark white, thin borders, warm clay #D85A30 accents, no shadows).Step-by-Step InstructionsStep 1: Project InitializationRun the command to create a new Flutter project in the current directory:flutter create family_wellbeing --platforms androidNavigate into the project: cd family_wellbeingStep 2: Native Android Setup (Kotlin)The core logic for reading screen time MUST be done natively in Kotlin because Flutter packages for screen time are unreliable and don't allow exact midnight-to-midnight bucketing.Open android/app/src/main/AndroidManifest.xml and add:<uses-permission android:name="android.permission.INTERNET" /><uses-permission android:name="android.permission.PACKAGE_USAGE_STATS" tools:ignore="ProtectedPermissions"/>Open android/app/build.gradle and ensure the minSdkVersion is at least 26.Add the Android Kotlin dependencies for WorkManager and MongoDB Coroutine driver in android/app/build.gradle.Create a MethodChannel in MainActivity.kt named com.family.wellbeing/stats.Implement the Kotlin UsageStatsExtractor and MongoDbSyncService (I will provide the Kotlin logic I have below. Please integrate it).Step 3: Flutter UI Implementation (Dart)Translate the provided React prototype into Flutter code (lib/main.dart).Use a BottomNavigationBar for Dashboard, Leaderboard, and Settings.Implement the "Daily vs Weekly" segmented control on the Leaderboard.Create expandable list tiles for the user rows to show the App Breakdown.Use the MethodChannel to call the native Kotlin code to trigger syncs and check permission status.Store the MongoDB URI locally using flutter_secure_storage or shared_preferences.Source Material provided by User1. Kotlin Logic (To be integrated into the Android folder):Use the concepts from this Kotlin code to build the Native side:Requires querying UsageStatsManager.INTERVAL_DAILY from Midnight to Midnight.Uses WorkManager for daily background syncing.Database records are stored with _id = "memberId_YYYY-MM-DD" to allow upserting incomplete today data, and overwriting it with complete data tomorrow.2. UI Design Rules (From the React Prototype):Background: #E5E5E5App Surface: #FAFAFAText: High contrast Zinc (#18181B for headings, #71717A for subtitles).Borders: Crisp, 1px solid borders (#E4E4E7). No heavy drop shadows.Accents: Warm Clay (#D85A30) for the leaderboard winner. Emerald green for success/completed states.Step 4: Build InstructionsOnce you have written the Dart and Kotlin code, provide the user with the exact terminal command to build the APK using the Flutter CLI:flutter build apk --releaseTell the user exactly where to find the .apk file (e.g., build/app/outputs/flutter-apk/app-release.apk) so they can install it on their phones.Now, please execute Step 1, Step 2, and Step 3, asking for my confirmation or providing the code files as you go.